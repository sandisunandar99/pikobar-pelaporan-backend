apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-pelaporan-backend.fullname" . }}-test-connection"
  labels:
    {{- include "helm-pelaporan-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-pelaporan-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
